pre-commit:
  parallel: true
  commands:
    format:
      glob: "*.{js,jsx,ts,tsx,json}"
      run: bunx oxfmt {staged_files}
      stage_fixed: true
    typecheck:
      run: bun run ts
    lint:
      run: bun run lint
    test:
      run: bun test
